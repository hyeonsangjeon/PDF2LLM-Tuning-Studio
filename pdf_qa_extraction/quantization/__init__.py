"""Quantization track (A: BF16 LoRA, B: INT4 PTQ, C: INT4 QAT).

Independent of the PDF extraction / persona / scorer tracks. Uses the standard
KorQuAD dataset (runtime download, not committed). See config.yaml and notebooks/.
"""
