"""PDF-native post-training workflow (independent orchestration package).

This package depends one-way on the stable interfaces exposed by ``pdf_qa``,
``evaluation`` and ``quantization``. Those packages never import from here.
"""
