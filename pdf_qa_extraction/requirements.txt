# ==========================================================================
# PDF2LLM-Tuning-Studio - PDF Q&A extraction container dependencies
# One image serves all providers (Azure AI Foundry / AWS Bedrock / OpenAI),
# so the same public image can feed both Azure ML and SageMaker.
# ==========================================================================

# --- Core ---
python-dotenv>=1.0
pydantic>=2
pyyaml>=6

# --- PDF partitioning (layout + OCR, CPU) ---
unstructured[pdf]>=0.15
opencv-python>=4.8
pdf2image>=1.17
pdfminer.six>=20250506
pytesseract>=0.3.10

# --- LLM orchestration ---
langchain-core>=0.3

# --- Provider: Azure AI Foundry (Azure OpenAI + Agent Service) ---
langchain-openai>=0.2
openai>=1.40
azure-identity>=1.17
azure-ai-projects>=1.0.0
azure-ai-agents>=1.0.0

# --- Provider: AWS Bedrock ---
langchain-aws>=0.2
boto3>=1.34
